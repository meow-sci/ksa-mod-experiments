# Feature library scaffold

Minimal example library for repository scaffolding. `FixmeModNameLib.Thing()` logs a sample message. This is a template, outside the shipping solution, and implements no game behavior or workspace feature.

New bundled features should follow [workspace extension rules](../docs/WORKSPACE.md#architecture-and-extension-rules), retain a separate library project, and register with the existing host.
