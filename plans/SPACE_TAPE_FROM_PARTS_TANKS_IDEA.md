use a fleet of 6

space tape is working!

however KSA has more detail in its parts then what it is supporting right now.

right now space-tape is doing basic GameData for weight and batter power, but it cannot for example:

- act as a tank for fuel
- define connectors (for connection points to other parts in the vehicle editor)
- start a new space-tape part design from an existing part (we should have a combobox to select a part to load into space-tape) so we can start from e.g. a tank and then add some new subparts to it and then save and have it be a fully working tank (with all GameData etc needed to be a tank, also pre-loaded when imported from an existing part)

do a deep dive into the ksa code in ksa/decomp as needed to see how this stuff works and make a detailed implementation plan in plans/SPACE_TAPE_FROM_PARTS_TANKS_PLAN.md of how to implement new features and capabilities to cover these features

the plan should be highly detailed with references to ksa decompiled sources as needed as reference and sufficient unambiguous detail so that a coding agent can take the task details and confidently implement the task as defined

ask me for any clarifications

