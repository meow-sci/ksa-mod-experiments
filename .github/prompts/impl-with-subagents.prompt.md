---
agent: agent

---

fully implement the #file:SPEC.md tasks using #runSubagent for each task, provide each subagent relevant contextual information to be able to achieve that particular task.

each task should not be complete until compilation passes.

the tasks CANNOT "test" this as it requires a human to validate, so just build each task as best as possible and I will test the final program manually.